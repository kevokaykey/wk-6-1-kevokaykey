// Early Stage Validation Tests
test('Basic test infrastructure works', () => {
  expect(1 + 1).toBe(2);
});

test('JavaScript environment is functional', () => {
  expect(typeof window).toBe('object');
});
